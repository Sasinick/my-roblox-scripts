local d = game.Players.LocalPlayer.Name
local uis = game:GetService("UserInputService")
workspace.Gravity = 0
game.Players.LocalPlayer.ReplicationFocus = Workspace
for _, a in pairs(workspace[d]:GetDescendants()) do
	if a:IsA("Accessory") then
		if a:FindFirstChild("Handle") then
a.handle.AccessoryWeld:Destroy()
uis.InputBegan:Connect(function(input, processsed)
if processed then return end
if input.KeyCode == Enum.KeyCode.E then
a.handle.Velocity = Vector3.new(0, 10, 0)
end
if input.KeyCode == Enum.KeyCode.Q then
a.handle.Velocity = Vector3.new(0, -10, 0)
end
if input.KeyCode == Enum.KeyCode.R then
a.handle.Velocity = Vector3.new(10, 0, 0)
end
if input.KeyCode == Enum.KeyCode.T then
a.handle.Velocity = Vector3.new(-10, 0, 0)
end
if input.KeyCode == Enum.KeyCode.Z then
a.handle.Velocity = Vector3.new(0, 0, 10)
end
if input.KeyCode == Enum.KeyCode.X then
a.handle.Velocity = Vector3.new(0, 0, -10)
end
if input.KeyCode == Enum.KeyCode.F then
a.handle.Velocity = Vector3.new(0, 0, 0)
end
end)
end		
while true do
if a:FindFirstChild("Handle") then
if a.Handle.ReceiveAge == 0 then
print("a")
workspace.Camera.CameraSubject = a.Handle
else
print("b")
end
end
task.wait(0.1)
end
		end
end
